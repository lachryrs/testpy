[project]
name = "migrations"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "alembic>=1.13",
    "sqlalchemy>=2.0",
    "psycopg2-binary>=2.9",
    "api",   # ← pulls in api as a dependency so env.py can import models
]

[tool.uv.sources]
api = { workspace = true }